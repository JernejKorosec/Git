import { Requestcredential } from '../classes/requestcredential';

describe('Requestcredential', () => {
  it('should create an instance', () => {
    expect(new Requestcredential()).toBeTruthy();
  });
});
